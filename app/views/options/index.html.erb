<div class="page-header">
<h1>Listing options</h1>
</div>


<table class="table">
  <tr>
    <th>Option</th>
    <th>Question</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @options.each do |option| %>
  <tr>
	<td><%= option.question.question %></td>
    <td><%= option.option %></td>
    
    <td><%= link_to 'Show', [option.question,option] %></td>
    <td><%= link_to 'Edit', edit_question_option_path(@question,option) %></td>
    <td><%= link_to 'Destroy', [option.question,option], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Option', new_option_path %>
